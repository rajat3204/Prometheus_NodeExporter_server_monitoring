Create prometheus.yml file under the prometheus folder . 
create prometheus folder in the current docker compose location.
And While you import the dashboard in Grafana. Use 1860 ID number to import . 